`$PLAN`: `/docs/plans/test-code/test-plan-05-security.md` 

Update backend and frontend security test codes by following `$PLAN`. Install necessary dependencies to ensure test runs.

implementation Notes:

- Extend existing `test_security.py` with enhanced OWASP Top 10 tests.
- Create the frontend security tests using the existing Vitest setup.

After that, review test codes with `/docs/code-review/test-code-review-guideline.md`, and loop through "refine & review" until no progress is made or user should decide to move forward.