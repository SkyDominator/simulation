# Review: Sync test-code-00-master -> test-plan-01-backend-unit (Iteration 01)

Date: 2025-09-18
Source: test-code-00-master.md (Backend Unit section tasks 1-14)
Target: test-plan-01-backend-unit.md

## Summary
Gaps identified between master backend unit task list (14 items) and target plan (28 tasks). Target contains additional tasks (fine if non-conflicting) but misses or contradicts several master-defined requirements. Adjustments needed to ensure every master task is explicitly represented and no contradictions remain.

## Gap Detail
| Master Task # | Master Description (Essential Point) | Target Status | Gap / Action Needed |
|---------------|---------------------------------------|---------------|---------------------|
| 1 | Fixtures: settings override, static JWKS loader, fake SMS provider | Present (Task 1) | OK |
| 2 | Per-plan invariants + 1‑round invariants | Present (Tasks 2, 5.2) | OK |
| 3 | Multi-round Plan A snapshot (non-negative cumulative metrics) | Present (Task 2 + 5.3) | Ensure explicit non-negative cumulative metric assertion retained |
| 4 | Edge cases: invalid plan id; rounds <= 0 | Present (Tasks 3, 5.4) | OK |
| 5 | Tax computation 3.3% first vs later rounds | Present (Task 5, 5.5) | OK |
| 6 | Achievement rate override path (if implemented) else Deferred | Present (Task 6, 5.6) | Add note for conditional (if not implemented) without introducing new section |
| 7 | Static JWKS parse + key selection test | Present (Task 7) | OK |
| 8 | Privacy policy fallback: simulate DB failure -> static markdown returned | Contradiction (Task 23 says "rejects static fallback") | Replace with fallback success behavior test |
| 9 | OTP verify attempt limit validation: 6 attempts | Partial (Task 25 constant guard only) | Add behavior test for limit logic (success then limit exceed) |
|10 | OTP send limiter (3 sends / 15m) helper logic | Missing | Add task & design section note |
|11 | Simulation input normalization: scheduled_payment -> investments jsonb conversion validation | Partial (Task 15 mapping) | Clarify mapping includes scheduled_payment to internal investments conversion |
|12 | Error response structure validation standard envelope | Present (Task 24) | OK |
|13 | JWKS caching behavior with TTL (5-15m) | Missing | Add task for caching TTL validation (time-freeze) |
|14 | Enhanced phone input formatting multi prefixes (010/011/016/017/018/019) | Partial (Task 20 phone/hash normalization) | Expand to explicitly cover multiple prefixes & formatting rules |

## Additional Target Tasks Not in Master
Settlement bonus rules, investor growth ceiling, snapshot versioning, extended JWT negative cases, determinism guard, consent/policy domain helpers, exception hierarchy, policy publish side-effect. These do not conflict with master; retain unless they introduce contradictions (none observed). Ensure they don't override master-defined behavior.

## Actions Required
1. Modify Task 23 to test fallback success scenario instead of rejection.
2. Add explicit task for OTP send limiter (3 sends / 15m) with time-freeze guidance.
3. Expand Task 25 (or split) to include functional OTP verify attempt limit behavior test (6 attempts).
4. Add JWKS caching TTL validation task using freeze_time (5-15m window) – new numbered task aligned near other JWKS tasks.
5. Clarify Task 15 (scheduled_payment mapping) to explicitly mention scheduled_payment -> investments jsonb conversion invariants.
6. Expand phone/hash normalization task to state multi-prefix formatting coverage (010/011/016/017/018/019) and rejection/normalization rules.
7. Note conditional nature of achievement rate override (if unimplemented treat as Deferred) without adding new sections.

## Conclusion
Gaps present; proceed to update target plan accordingly.
