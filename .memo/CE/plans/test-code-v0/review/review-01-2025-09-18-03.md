# Review 03 – Backend Unit Test Plan (Post-Decision Updates)
Date: 2025-09-18
Index: 03

## Summary
Decisions applied (achievement override rule, settlement bonus range, exclusions sentence, snapshot note). Remaining NEED_DECISION items reduced. Minor formatting (Markdown lint) issues persist (list spacing, fenced code blank lines, indentation). Content-wise, only unresolved items are those still listed in Pending Decisions table. No new semantic inconsistencies introduced.

## Issues

### 1. Markdown Formatting (Non-semantic)
- Location: Various (Objectives list, Environment list, fenced code around clock fixture, nested list in Section 5.6, etc.)
- Issue: MD022/MD032/MD031/MD007 lint violations (blank lines around headings/lists/fences; list indent)
- Suggestion: Insert blank lines before/after headings and lists; ensure fenced code blocks surrounded by blank lines; adjust nested list indentation to two spaces. (Pure formatting; optional if repo lint does not enforce.)

### 2. Pending Decisions Table Alignment with SSD
- Location: Section 7.1
- Issue: D8 JWKS cache TTL still states confirmation needed while SSD Section 7.1 earlier states "Cache keys (TTL 5–15m)"; potential divergence when fixed TTL chosen (10m proposed).
- Suggestion: Either (a) adopt fixed 10m TTL in SSD and mark D8 resolved or (b) keep dynamic window and adjust test plan to reflect range (skip strict TTL test). Tag stays until resolved.
- TAG: NEED_DECISION

### 3. Plan ID Normalization (D3)
- Location: Section 7.1
- Issue: Still pending; if implementation already rejects lowercase without normalization, decision should note whether normalization occurs before validation.
- Suggestion: Inspect code (constants / input layer) then finalize; remove from table.
- TAG: NEED_DECISION

### 4. Duplicate Scheduled Payments Aggregation (D4)
- Location: Section 7.1
- Issue: Not specified in current service code (service overwrites via dict) so summation rule may require pre-coercion step.
- Suggestion: Decide to forbid duplicates (reject) OR accept first/last wins (document) OR implement summation. Until chosen keep NEED_DECISION.
- TAG: NEED_DECISION

### 5. Determinism Guard Scope (D5)
- Location: Section 7.1
- Issue: Decide if include `secrets`, `uuid`, or external randomness sources.
- Suggestion: Minimal viable: random + numpy. Expand only if relevant calls observed.
- TAG: NEED_DECISION

### 6. Consent Version Cache Helper (D6)
- Location: Section 7.1
- Issue: SSD does not mention cache helper explicitly.
- Suggestion: Either remove if not planned or add brief SSD note; finalize TTL.
- TAG: NEED_DECISION

### 7. OTP Send Limiter Window (D7)
- Location: Section 7.1
- Issue: Confirm backend implementation supports rolling window, else adjust to fixed bucket.
- Suggestion: Inspect rate limiter code then finalize.
- TAG: NEED_DECISION

### 8. Simulation List Normalization Function Name (D9)
- Location: Section 7.1
- Issue: Provide actual utility name or mark out-of-scope if integration-layer only.
- TAG: NEED_DECISION

### 9. Exception Base Class Name (D10)
- Location: Section 7.1
- Issue: Confirm presence (e.g., DomainError / AppError) else adjust plan's item to specific existing classes.
- TAG: NEED_DECISION

### 10. Policy Publish Side-effect Flag (D11)
- Location: Section 7.1
- Issue: If not yet implemented, consider deferring test until implemented; currently optional.
- TAG: NEED_DECISION

### 11. Snapshot Rounds Constant Symbol (D12)
- Location: Section 7.1
- Issue: Decision whether to introduce symbolic constant purely in tests; not required.
- Suggestion: If added, local constant inside test module; else remove item.
- TAG: NEED_DECISION

## Conclusion
Only formatting + pending decision housekeeping remain. Once decisions D3–D12 settled (or removed) and formatting cleaned, review loop can terminate with no outstanding issues.
